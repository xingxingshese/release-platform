# 011-kubernetes-deployment

状态：⬜ 待 Phase 13 填充
范围：DeploymentAdapter/KubernetesAdapter、多 Pod 成功条件（desired==updated==ready==available 且 unavailable==0）、ReleaseDeploymentNode 记录、HealthCheckConfig/VersionCheckConfig 执行器、ReleaseSuccessEvaluator、超时。ADR-006。TDD 用例基线见 ADR-006。
